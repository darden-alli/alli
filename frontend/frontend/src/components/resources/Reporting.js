import withRoot from '../landingPage/modules/withRoot';
// --- Post bootstrap -----
import React from 'react';

function Reporting(props) {
  return(
    <h1>Reporting</h1>
  )
}

export default withRoot(Reporting);

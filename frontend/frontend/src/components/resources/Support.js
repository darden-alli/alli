import withRoot from '../landingPage/modules/withRoot';
// --- Post bootstrap -----
import React from 'react';

function Support(props) {
  return(
    <h1>Support</h1>
  )
}

export default withRoot(Support);

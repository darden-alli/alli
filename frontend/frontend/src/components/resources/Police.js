import withRoot from '../landingPage/modules/withRoot';
// --- Post bootstrap -----
import React from 'react';

function Police(props) {
  return(
    <h1>Police</h1>
  )
}

export default withRoot(Police);

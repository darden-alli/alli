import withRoot from '../landingPage/modules/withRoot';
// --- Post bootstrap -----
import React from 'react';

function Medical(props) {
  return(
    <h1>Medical</h1>
  )
}

export default withRoot(Medical);
